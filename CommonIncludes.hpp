#pragma once
#include <vector>            // For `std::vector`.
#include <utility>           // For variadic templates.
#include <iostream>          // For `std::cout`.
#include <initializer_list>  // For `std::initializer_list`.